import { PokedexConverterPipe } from './pokedex-converter-pipe';

describe('PokedexConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new PokedexConverterPipe();
    expect(pipe).toBeTruthy();
  });
});

import { StringCapitalisePipe } from './string-capitalise-pipe';

describe('StringCapitalisePipe', () => {
  it('create an instance', () => {
    const pipe = new StringCapitalisePipe();
    expect(pipe).toBeTruthy();
  });
});
